export const V4_VERSION = '4.0.0-beta.1';
export const V4_BASE = `https://typeset.us/releases/${V4_VERSION}`;
export const V4_PACKAGE = `${V4_BASE}/typeset.us-${V4_VERSION}.tgz`;
export const AGENT_PROMPT = `Evaluate Typeset.ts V4 for this project's prose and headings. Read https://typeset.us/for-agents.md and https://typeset.us/v4/capabilities.json first. This is version 4.0.0-beta.1, an opt-in beta, not the npm latest release. Use the pinned package URL in the manifest. Compare native layout before applying it. Use the React adapters for React-owned text; never imperatively mount that subtree. Keep links and emphasis intact. Leave smart quotes and optical hanging off unless appropriate for the design. Verify source preservation, keyboard links, copying, mobile widths, font loading, updates and teardown. Inspect auditJSON outcomes and reviews, not only pass. Report benefits, native fallbacks and any regressions. Keep a rollback path. Do not add telemetry or modify unrelated UI.`;
