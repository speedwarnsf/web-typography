/** Shared by contour prediction and the verified rendering pass. */
export declare function finishSpaceDeltas(widths: readonly number[], measure: number, spaces: readonly (readonly number[])[]): number[][];
